---
title: General knowledge question - fixed trivia cards
content: Implement the general-knowledge question handler. The CLI must answer a small fixed set of trivia questions (Theresa May election year, Eiffel tower city, banana colour, James Bond in Dr No). Worth 10 points.
tags:
  - trivia
  - cli
intervals:
  deadline_secs: 2
  min_interval_secs: 30
  interval_increment_secs: 10
  max_interval_secs: 300
test_template:
  kind: shell
  placeholders:
    - name: baseDir
      description: Auto-detected placeholder 'baseDir'
      required: false
      secret: false
    - name: id
      description: Auto-detected placeholder 'id'
      required: false
      secret: false
    - name: question
      description: The trivia question text
      required: false
      secret: false
  matchers:
    expected_exit_code: 0
    max_duration_ms: 60000
    param_timeout_ms: 60000
  backoff:
    initial_ms: 1000
    multiplier: 2
    max_ms: 30000
    max_attempts: 5
  fixtures: []
  answer_template: null
---

## Answer the Eiffel tower city question

Ask "which city is the Eiffel tower in"; the answer is "Paris".

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh",
  question: "which city is the Eiffel tower in",
  answer: "Paris",
  __secret: ["answer"],
  __secret_expected: true
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the Eiffel tower city question and trim whitespace
$R -q "{id}: {question}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
result.trim().toLowerCase() === answer.toLowerCase()
```

## Answer the banana colour question

Ask "what colour is a banana"; the answer is "yellow".

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh",
  question: "what colour is a banana",
  answer: "yellow",
  __secret: ["answer"],
  __secret_expected: true
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the banana colour question and trim whitespace
$R -q "{id}: {question}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
result.trim().toLowerCase() === answer.toLowerCase()
```

## Answer the James Bond Dr No question

Ask "who played James Bond in the film Dr No"; the answer is "Sean Connery".

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh",
  question: "who played James Bond in the film Dr No",
  answer: "Sean Connery",
  __secret: ["answer"],
  __secret_expected: true
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the James Bond Dr No question and trim whitespace
$R -q "{id}: {question}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
result.trim().toLowerCase() === answer.toLowerCase()
```

## Answer the Theresa May election year question

Ask "which year was Theresa May first elected as the Prime Minister of Great Britain"; the answer is "2016".

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh",
  question: "which year was Theresa May first elected as the Prime Minister of Great Britain",
  answer: "2016",
  __secret: ["answer"],
  __secret_expected: true
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the Theresa May election year question and trim whitespace
$R -q "{id}: {question}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
assertEqual(result.trim(), answer)
```
