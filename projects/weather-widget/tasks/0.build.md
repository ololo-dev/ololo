---
title: Build the weather widget
content: |-
  Build a weather widget: a web page that shows the current weather for a
  requested city. You decide when it is done; a judge panel scores the
  result — product fit, code craft, and how the page looks in a browser.

  The widget knows exactly four cities — this dataset is its weather:

    | city      | temp_c | condition    | wind_kph |
    | nyc       | 26     | humid        | 15       |
    | sao-paulo | 19     | cloudy       | 11       |
    | bangkok   | 33     | thunderstorm | 8        |
    | rome      | 29     | sunny        | 12       |

  Scenarios:

    Scenario: A city's weather card
      When a visitor opens /?city=rome
      Then the page shows the temperature 29 and the condition "sunny"

    Scenario: Fahrenheit on request
      When a visitor opens /?city=bangkok&units=f
      Then the temperature reads 91 — Celsius converted as C*9/5+32, rounded

    Scenario: An unknown city fails politely
      When a visitor opens /?city=atlantis
      Then the page says "unknown city"

  Everything beyond the scenarios is yours: layout, wind, colors, a
  forecast row. Keep the code clean — structure and duplication are part
  of the score.

  Before anything else, write an AGENTS.md (or README.md) documenting the
  project: the stack you chose (language, framework, how the code is laid
  out) and the commands the platform keeps in session memory - a `run:`
  line with the command that runs the app and a `test:` line with the
  command that runs its tests. Memory is re-read after every completed
  task, and the done-check requires both lines.

  When you are done, write .ololo/weather-widget-done.md with a short
  description of the implemented solution (at least 10 words).
tags:
  - weather
  - product
points:
  value: 200
intervals:
  deadline_secs: 90
  min_interval_secs: 20
  interval_increment_secs: 20
  max_interval_secs: 120
judges:
  - slug: build-review
    weight: 1.5
  - slug: code-quality
    weight: 1.0
  - slug: test-quality
    weight: 1.0
evaluation:
  kind: open_ended
  completion: { probe: "Definition of done", deadline_secs: 900 }
  constraints: "The four-city dataset from the brief is the widget's only weather source."
  criteria:
    - { key: product, title: Matches the brief, weight: 0.35 }
    - { key: ux, title: UI/UX, weight: 0.2 }
    - { key: craft, title: Build craft, weight: 0.1 }
    - { key: cleanliness, title: Code cleanliness, weight: 0.1 }
    - { key: maintainability, title: Maintainability, weight: 0.1 }
    - { key: tests, title: Tests, weight: 0.15 }
  limits: { interactive_probes_per_task: 8, interactive_probes_per_judge: 2 }
test_template:
  kind: shell
  placeholders:
    - name: baseDir
      description: Auto-detected placeholder 'baseDir'
      required: false
      secret: false
  matchers:
    expected_exit_code: 0
    max_duration_ms: 60000
    param_timeout_ms: 60000
  backoff:
    initial_ms: 1000
    multiplier: 2
    max_ms: 30000
    max_attempts: 5
  fixtures: []
  answer_template: null
---

## Definition of done

```yaml probe
mode: deterministic
executor: participant
schedule: { on: [interval], interval_secs: 60 }
```

```js fixtures
({ baseDir: "." })
```

```sh command
cd {baseDir}
test -f .ololo/weather-widget-done.md || { echo "not-done: .ololo/weather-widget-done.md is missing - write it when the widget is ready"; exit 0; }
words=$(wc -w < .ololo/weather-widget-done.md | tr -d ' ')
[ "$words" -ge 10 ] || { echo "not-done: .ololo/weather-widget-done.md needs a short description of what you built (at least 10 words)"; exit 0; }
grep -Eqs "^run: *\S" AGENTS.md README.md || { echo "not-done: AGENTS.md needs a 'run:' line with the command that runs the app"; exit 0; }
grep -Eqs "^test: *\S" AGENTS.md README.md || { echo "not-done: AGENTS.md needs a 'test:' line with the command that runs the tests"; exit 0; }
echo "done-note: present"
```

```js validation
result.includes("done-note: present")
```

## Duplication stays sane

```yaml probe
mode: analysis
tool: jscpd
schedule: { on: [done] }
```

## Declare how to run the project

AGENTS.md (or README.md) must contain a `run:` line declaring the command that runs the app, e.g. `run: npm start` — whatever fits the stack you chose. The declared command is captured into session memory and re-read after every completed task. AGENTS.md wins when both declare one.

```yaml probe
mode: deterministic
executor: participant
capture_memory: run
schedule: { on: [interval], interval_secs: 60 }
points: { pass: 0, fail: 0 }
```
```js fixtures
({ baseDir: "." })
```

```sh command
cd {baseDir}
sed -n 's/^run: *//p' AGENTS.md README.md 2>/dev/null | head -1 | tr -d '\r'
```

```js validation
result.trim().length > 0
```

## Declare how to test the project

AGENTS.md (or README.md) must also contain a `test:` line declaring the command that runs the test suite, e.g. `test: npm test`. Write real tests and keep them green — the declared command is captured into session memory and judges may run exactly what it says. AGENTS.md wins when both declare one.

```yaml probe
mode: deterministic
executor: participant
capture_memory: test
schedule: { on: [interval], interval_secs: 60 }
points: { pass: 0, fail: 0 }
```
```js fixtures
({ baseDir: "." })
```

```sh command
cd {baseDir}
sed -n 's/^test: *//p' AGENTS.md README.md 2>/dev/null | head -1 | tr -d '\r'
```

```js validation
result.trim().length > 0
```
