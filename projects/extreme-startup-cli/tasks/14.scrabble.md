---
title: Scrabble question - what is the english scrabble score of X
content: Implement the Scrabble score question handler. When the question is "what is the english scrabble score of {word}", respond with the standard English Scrabble score of the word using the letter-values table (1=eaionrtlsu, 2=dg, 3=bcmp, 4=fhvwy, 5=k, 8=jx, 10=qz). Worth 10 points. The word is sampled from a fixed list (banana, september, cloud, zoo, ruby, buzzword).
tags:
  - word
  - cli
intervals:
  deadline_secs: 2
  min_interval_secs: 30
  interval_increment_secs: 10
  max_interval_secs: 300
test_template:
  kind: shell
  placeholders:
    - name: baseDir
      description: Auto-detected placeholder 'baseDir'
      required: false
      secret: false
    - name: id
      description: Auto-detected placeholder 'id'
      required: false
      secret: false
    - name: word
      description: The word to score
      required: false
      secret: false
  matchers:
    expected_exit_code: 0
    max_duration_ms: 60000
    param_timeout_ms: 60000
  backoff:
    initial_ms: 1000
    multiplier: 2
    max_ms: 30000
    max_attempts: 5
  fixtures: []
  answer_template: null
---

## Answer the Scrabble score question with derived word and score

Sample a word from the fixed list (banana, september, cloud, zoo, ruby, buzzword), compute its standard English Scrabble score using an IIFE in the fixtures object, and ask "what is the english scrabble score of {word}". The response must equal the score.

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh",
  ...((word) => ({ word, score: word.split("").reduce((s, ch) => s + ({e:1,a:1,i:1,o:1,n:1,r:1,t:1,l:1,s:1,u:1,d:2,g:2,b:3,c:3,m:3,p:3,f:4,h:4,v:4,w:4,y:4,k:5,j:8,x:8,q:10,z:10}[ch] || 0), 0) }))((["banana","september","cloud","zoo","ruby","buzzword"])[Math.floor(Math.random() * 6)])
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the Scrabble score question and trim whitespace
$R -q "{id}: what is the english scrabble score of {word}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
assertEqual(Number(result.trim()), score)
```
