---
title: Code & UX review — the craft of your solution
content: |-
  You have a working single-page app that answers every question type in
  the browser. This final task reviews how it is BUILT and how it LOOKS,
  not what it answers: a judge panel scores the code's cleanliness and
  maintainability, the meaningfulness of your tests, and the UI/UX of the
  running app — visual quality, accessibility, and mobile readiness.
  Correctness was already earned earlier; here the points are for craft.

  Tidy the code, add tests that exercise the answer logic, and make the
  page pleasant and accessible. The UX reviewer may ask for a screenshot
  of the running app — keep it serveable so you can deliver one. When you
  are satisfied, write `.ololo/extreme-startup-browser-done.md` with a
  short description of your solution and how it is organized (at least 10
  words) to submit it for review.
tags:
  - review
  - delivery
points:
  value: 90
intervals:
  deadline_secs: 90
  min_interval_secs: 20
  interval_increment_secs: 20
  max_interval_secs: 120
judges:
  - slug: code-quality
    weight: 1.0
  - slug: test-quality
    weight: 1.0
evaluation:
  kind: open_ended
  completion: { probe: "Definition of done", deadline_secs: 600 }
  constraints: "Judged on the single-page-app solution you built across the earlier tasks; correctness is not re-scored here."
  criteria:
    - { key: cleanliness, title: Code cleanliness, weight: 0.2 }
    - { key: maintainability, title: Maintainability, weight: 0.2 }
    - { key: tests, title: Tests, weight: 0.2 }
    - { key: ux, title: UI/UX, weight: 0.2 }
    - { key: accessibility, title: Accessibility, weight: 0.1 }
    - { key: mobile, title: Mobile readiness, weight: 0.1 }
  limits: { interactive_probes_per_task: 6, interactive_probes_per_judge: 2 }
test_template:
  kind: shell
  placeholders:
    - name: baseDir
      description: Auto-detected placeholder 'baseDir'
      required: false
      secret: false
  matchers:
    expected_exit_code: 0
    max_duration_ms: 60000
    param_timeout_ms: 60000
  backoff:
    initial_ms: 1000
    multiplier: 2
    max_ms: 30000
    max_attempts: 5
  fixtures: []
  answer_template: null
---

## Definition of done

```yaml probe
mode: deterministic
executor: participant
schedule: { on: [interval], interval_secs: 60 }
```

```js fixtures
({ baseDir: "." })
```

```sh command
cd {baseDir}
test -f .ololo/extreme-startup-browser-done.md || { echo "not-done: .ololo/extreme-startup-browser-done.md is missing - write it when your solution is ready for review"; exit 0; }
words=$(wc -w < .ololo/extreme-startup-browser-done.md | tr -d ' ')
[ "$words" -ge 10 ] || { echo "not-done: .ololo/extreme-startup-browser-done.md needs a short description of your solution (at least 10 words)"; exit 0; }
echo "done-note: present"
```

```js validation
result.includes("done-note: present")
```

## Duplication stays sane

```yaml probe
mode: analysis
tool: jscpd
schedule: { on: [done] }
```
