<!--
  This route only redirects (see +page.server.ts) to /s/<code>; the load
  throws before render, so this component never actually shows. It exists
  because SvelteKit requires a +page.svelte for a page route.
-->
<script lang="ts">
</script>
