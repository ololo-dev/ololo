---
title: Addition question - what is X plus Y
content: Implement the addition question handler. When the question is "what is {n1} plus {n2}", respond with the integer sum. Worth 10 points.
tags:
  - arithmetic
  - cli
intervals:
  deadline_secs: 2
  min_interval_secs: 30
  interval_increment_secs: 10
  max_interval_secs: 300
test_template:
  kind: shell
  placeholders:
    - name: baseDir
      description: Auto-detected placeholder 'baseDir'
      required: false
      secret: false
    - name: id
      description: Auto-detected placeholder 'id'
      required: false
      secret: false
    - name: n1
      description: Auto-detected placeholder 'n1'
      required: false
      secret: false
    - name: n2
      description: Auto-detected placeholder 'n2'
      required: false
      secret: false
  matchers:
    expected_exit_code: 0
    max_duration_ms: 60000
    param_timeout_ms: 60000
  backoff:
    initial_ms: 1000
    multiplier: 2
    max_ms: 30000
    max_attempts: 5
  fixtures: []
  answer_template: null
---

## Answer the addition question with the correct sum

Ask "what is {n1} plus {n2}" and verify the response equals n1+n2. Both operands are sampled from [0, 20].

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh",
  n1: Math.floor(Math.random() * 21),
  n2: Math.floor(Math.random() * 21)
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the addition question and trim whitespace
$R -q "{id}: what is {n1} plus {n2}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
assertEqual(Number(result.trim()), n1 + n2)
```
