---
title: Warmup question - what is your name
content: Implement the warmup question handler. When the question is "what is your name", the CLI must respond with the participant's name. Worth 10 points, and the first question every player must answer.
tags:
  - warmup
  - cli
intervals:
  deadline_secs: 2
  min_interval_secs: 30
  interval_increment_secs: 10
  max_interval_secs: 300
test_template:
  kind: shell
  placeholders:
    - name: baseDir
      description: Auto-detected placeholder 'baseDir'
      required: false
      secret: false
    - name: id
      description: Auto-detected placeholder 'id'
      required: false
      secret: false
  matchers:
    expected_exit_code: 0
    max_duration_ms: 60000
    param_timeout_ms: 60000
  backoff:
    initial_ms: 1000
    multiplier: 2
    max_ms: 30000
    max_attempts: 5
  fixtures: []
  answer_template: null
---

## Answer the warmup question "what is your name"

Send the canonical warmup question `{id}: what is your name` and verify the response is non-empty. Any non-empty trimmed response passes.

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh"
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the warmup question and trim whitespace
$R -q "{id}: what is your name" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
result.trim().length > 0
```
