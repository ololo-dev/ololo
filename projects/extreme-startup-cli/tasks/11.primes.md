---
title: Primes question - which of the following numbers are primes
content: Implement the primes question handler. Given a comma-separated list of numbers, respond with those that are prime. Worth 60 points. Candidate pool mixes small primes with random composites.
tags:
  - selection
  - number-theory
  - cli
intervals:
  deadline_secs: 2
  min_interval_secs: 30
  interval_increment_secs: 10
  max_interval_secs: 300
points:
  value: 60
test_template:
  kind: shell
  placeholders:
    - name: baseDir
      description: Auto-detected placeholder 'baseDir'
      required: false
      secret: false
    - name: id
      description: Auto-detected placeholder 'id'
      required: false
      secret: false
    - name: list
      description: Comma-joined candidate numbers
      required: false
      secret: false
  matchers:
    expected_exit_code: 0
    max_duration_ms: 60000
    param_timeout_ms: 60000
  backoff:
    initial_ms: 1000
    multiplier: 2
    max_ms: 30000
    max_attempts: 5
  fixtures: []
  answer_template: null
---

## Answer the primes question with the prime numbers from the list

Build a candidate list mixing small primes with obvious composites. Ask "which of the following numbers are primes: ...". The response must list exactly the primes, in any order. Compared as sorted sets.

```js fixtures
({
  baseDir: ".",
  id: Math.random().toString(36).slice(2, 10),
  command: "answer.sh",
  list: [2, 3, 5, 7, 11, 13, 17, 4, 6, 8, 9, 10, 15, 21, 25].sort(() => Math.random() - 0.5).join(", ")
})
```

```sh command
# Enter the project directory
cd {baseDir}
mkdir -p .ololo/tmp

# Run command from session memory (LLM-extracted from AGENTS.md/README.md;
# arrives shell-quoted, so assign then invoke via word-splitting $R)
R={memory.run}

# Ask the primes question and trim whitespace
$R -q "{id}: which of the following numbers are primes: {list}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//'
```

```js validation
const isPrime = n => { if (n < 2) return false; for (let d = 2; d*d <= n; d++) if (n % d === 0) return false; return true; };
const nums = list.split(",").map(s => Number(s.trim()));
const expected = nums.filter(isPrime).sort((a,b)=>a-b).join(", ");
const got = result.trim().split(",").map(s => Number(s.trim())).sort((a,b)=>a-b).join(", ");
return assertEqual(got, expected)
```
